rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /purchases/{docId} {
      // Public read — anyone can view purchases
      allow read: if true;
      // Write requires auth (site owner only)
      allow write, delete: if request.auth != null;
    }
  }
}
